#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
const char nl='\n';
using vi = vector<int>;

int main()
{
    ll x,y;
    cin>>x>>y;

    ll add=x+y;
    ll mul=x*y;
    ll sub=x-y;

    cout<<x<<" + "<<y<<" = "<<add<<nl;
    cout<<x<<" * "<<y<<" = "<<mul<<nl;
    cout<<x<<" - "<<y<<" = "<<sub<<nl;




}
