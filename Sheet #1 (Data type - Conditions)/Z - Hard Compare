#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
const char nl='\n';
using vi = vector<int>;

int main()
{

    ll a,b,c,d;
    cin>>a>>b>>c>>d;

    long double x=b*logl(a);
    long double y=d*logl(c);

    if(x>y)
    {
        cout<<"YES"<<nl;
    }
    else
    {
        cout<<"NO"<<nl;
    }




}
