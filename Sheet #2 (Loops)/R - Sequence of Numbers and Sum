#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
const char nl='\n';
using vi = vector<int>;

int main()
{

    int n,m;

    while(cin>>n>>m)
    {

        if(n<=0 || m<=0)
            break;

        int s=0;
        if(n>m)
        {
            for(int i=m;i<=n;i++)
            {
                cout<<i<<" ";
                s+=i;
            }
        }
        else
        {
            for(int i=n;i<=m;i++)
            {
                cout<<i<<" ";
                s+=i;
            }
        }

        cout<<"sum ="<<s<<nl;

    }



}
