#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
const char nl='\n';
using vi = vector<int>;

int main()
{
    int n,x;
    cin>>n;

    int a[100000];
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
    }
    cin>>x;

    for(int i=0;i<n;i++)
    {
        if(a[i]==x)
        {
            cout<<i<<nl;
            return 0;
        }
    }

    cout<<-1<<nl;

}
