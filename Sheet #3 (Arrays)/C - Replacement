#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
const char nl='\n';
using vi = vector<int>;

int main()
{
    int n;
    cin>>n;

    int a[1000];

    for(int i=0;i<n;i++)
    {
        cin>>a[i];
        if(a[i]>0)
        {
            a[i]=1;
        }
        else if(a[i]<0)
        {
            a[i]=2;
        }
    }

    for(int i=0;i<n;i++)
    {
        cout<<a[i]<<" ";
    }

}
