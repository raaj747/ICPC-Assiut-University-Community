#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
const char nl='\n';
using vi = vector<int>;

int main()
{

    int n;
    cin>>n;

    if(n==1)
    {
        cout<<0;
        return 0;
    }
    if(n==2)
    {
        cout<<1;
        return 0;
    }

    ll a=0,b=1,c;

    for(int i=3;i<=n;i++)
    {
        c=a+b;
        a=b;
        b=c;
    }

    cout<<c;



}
