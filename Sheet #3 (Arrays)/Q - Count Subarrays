#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
const char nl='\n';
using vi = vector<int>;

int main()
{

    int t;
    cin>>t;
    while(t--)
    {
        int n;
        cin>>n;

        int a[102];

        for(int i=0;i<n;i++)
        {
            cin>>a[i];
        }

        int b=0;

        for(int i=0;i<n;i++)
        {
            int c=a[i];
            for(int j=i;j<n;j++)
            {
                if(j>i && a[j]<a[j-1])
                    break;
                b++;
            }
        }

        cout<<b<<nl;
    }



}
